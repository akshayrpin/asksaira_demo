"""solr_core — generic, collection-agnostic Solr plumbing shared by city-staff agents.

Nothing here is Burbank- or permit-specific: field names, types, and value sets are all
discovered per collection (see schema.py). Lifted/generalized from permit_agent/permit_client.py.
"""
